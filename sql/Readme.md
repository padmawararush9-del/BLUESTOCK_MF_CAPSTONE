This folder contains SQL queries used during the Mutual Fund Analytics project for data exploration, validation, and analysis.

The queries were used to:

Retrieve and filter mutual fund data
Perform aggregations and calculations
Validate processed datasets
Support analytical reporting

The SQL scripts complement the Python-based ETL and analytics workflow by enabling efficient data querying and verification.